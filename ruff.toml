[tool.ruff]
target-version = "py310"
line-length = 100
select = ["E", "F", "W", "I", "B", "C4", "UP"]
ignore = ["E501", "B008"]

[tool.ruff.per-file-ignores]
"tests/*" = ["S101"]  # Allow assert in tests
"dags/*" = ["E402"]   # Allow module-level imports not at top of file
