# DO NOT commit actual secrets. Use this as a template.
# Create via: kubectl create secret generic mlops-secrets --from-env-file=.env -n mlops
apiVersion: v1
kind: Secret
metadata:
  name: mlops-secrets
  namespace: mlops
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "CHANGE_ME"
  AWS_SECRET_ACCESS_KEY: "CHANGE_ME"
  POSTGRES_PASSWORD: "CHANGE_ME"
